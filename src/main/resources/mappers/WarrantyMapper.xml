<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.WarrantyMapper">

    <insert id="insertWarranty" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO warranties (provider, expiration_date, asset_id, is_active)
        VALUES (#{provider}, #{expirationDate}, #{assetId}, #{isActive})
    </insert>

    <select id="getWarrantyById" resultType="Warranty">
        SELECT * FROM warranties WHERE id = #{id} AND is_active = true
    </select>

    <select id="getWarrantyByAssetId" resultType="Warranty">
        SELECT * FROM warranties WHERE asset_id = #{assetId} AND is_active = true
    </select>

    <select id="getAllWarranties" resultType="Warranty">
        SELECT * FROM warranties WHERE is_active = true
    </select>

    <update id="updateWarranty">
        UPDATE warranties
        SET provider = #{provider},
        expiration_date = #{expirationDate},
        asset_id = #{assetId},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

</mapper>