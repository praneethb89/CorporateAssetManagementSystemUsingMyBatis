<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.AssetAllocationMapper">

    <insert id="insertAllocation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO asset_allocations (allocation_date, return_date, remarks, asset_id, employee_id, is_active)
        VALUES (#{allocationDate}, #{returnDate}, #{remarks}, #{assetId}, #{employeeId}, #{isActive})
    </insert>

    <select id="getAllocationById" resultType="AssetAllocation">
        SELECT * FROM asset_allocations WHERE id = #{id} AND is_active = true
    </select>

    <select id="getAllAllocations" resultType="AssetAllocation">
        SELECT * FROM asset_allocations WHERE is_active = true
    </select>

    <select id="getAllocationsByEmployeeId" resultType="AssetAllocation">
        SELECT * FROM asset_allocations WHERE employee_id = #{employeeId} AND is_active = true
    </select>

    <select id="getAllocationsByAssetId" resultType="AssetAllocation">
        SELECT * FROM asset_allocations WHERE asset_id = #{assetId} AND is_active = true
    </select>

    <update id="updateAllocation">
        UPDATE asset_allocations
        SET allocation_date = #{allocationDate},
        return_date = #{returnDate},
        remarks = #{remarks},
        asset_id = #{assetId},
        employee_id = #{employeeId},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

</mapper>