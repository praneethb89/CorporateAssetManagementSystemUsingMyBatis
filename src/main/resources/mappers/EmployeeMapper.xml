<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.EmployeeMapper">

    <!-- This explicitly maps SQL columns (with underscores) to Java fields (camelCase) -->
    <resultMap id="EmployeeResultMap" type="Employee">
        <id property="id" column="id"/>
        <result property="employeeId" column="employee_id"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName" column="last_name"/>
        <result property="email" column="email"/>
        <result property="department" column="department"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- Updated to insert first_name and last_name -->
    <insert id="insertEmployee" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employees (employee_id, first_name, last_name, email, department, password, role, is_active)
        VALUES (#{employeeId}, #{firstName}, #{lastName}, #{email}, #{department}, #{password}, #{role}, #{isActive})
    </insert>

    <!-- Updated all selects to use the new resultMap -->
    <select id="getEmployeeById" resultMap="EmployeeResultMap">
        SELECT * FROM employees WHERE id = #{id} AND is_active = true
    </select>

    <select id="getEmployeeByEmployeeId" resultMap="EmployeeResultMap">
        SELECT * FROM employees WHERE employee_id = #{employeeId} AND is_active = true
    </select>

    <select id="getAllEmployees" resultMap="EmployeeResultMap">
        SELECT * FROM employees WHERE is_active = true
    </select>

    <!-- Updated to update first_name and last_name -->
    <update id="updateEmployee">
        UPDATE employees
        SET first_name = #{firstName},
        last_name = #{lastName},
        email = #{email},
        department = #{department},
        password = #{password},
        role = #{role},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

</mapper>