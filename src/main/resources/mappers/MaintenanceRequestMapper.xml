<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.MaintenanceRequestMapper">

    <insert id="insertMaintenanceRequest" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO maintenance_requests (issue_description, cost, status, asset_id, is_active)
        VALUES (#{issueDescription}, #{cost}, #{status}, #{assetId}, #{isActive})
    </insert>

    <select id="getMaintenanceRequestById" resultType="MaintenanceRequest">
        SELECT * FROM maintenance_requests WHERE id = #{id} AND is_active = true
    </select>

    <select id="getAllMaintenanceRequests" resultType="MaintenanceRequest">
        SELECT * FROM maintenance_requests WHERE is_active = true
    </select>

    <select id="getMaintenanceRequestsByAssetId" resultType="MaintenanceRequest">
        SELECT * FROM maintenance_requests WHERE asset_id = #{assetId} AND is_active = true
    </select>

    <update id="updateMaintenanceRequest">
        UPDATE maintenance_requests
        SET issue_description = #{issueDescription},
        cost = #{cost},
        status = #{status},
        asset_id = #{assetId},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

</mapper>