<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.VendorMapper">

    <insert id="insertVendor" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vendors (vendor_name, contact_email, is_active)
        VALUES (#{vendorName}, #{contactEmail}, #{isActive})
    </insert>

    <select id="getVendorById" resultType="Vendor">
        SELECT * FROM vendors WHERE id = #{id} AND is_active = true
    </select>

    <select id="getAllVendors" resultType="Vendor">
        SELECT * FROM vendors WHERE is_active = true
    </select>

    <update id="updateVendor">
        UPDATE vendors
        SET vendor_name = #{vendorName},
        contact_email = #{contactEmail},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <update id="deleteVendor">
        UPDATE vendors SET is_active = false WHERE id = #{id}
    </update>

</mapper>