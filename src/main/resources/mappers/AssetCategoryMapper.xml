<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.corporateassetmanagementsystem.mapper.AssetCategoryMapper">

    <insert id="insertAssetCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO asset_categories (category_name, description, is_active)
        VALUES (#{categoryName}, #{description}, #{isActive})
    </insert>

    <select id="getAssetCategoryById" resultType="AssetCategory">
        SELECT * FROM asset_categories WHERE id = #{id} AND is_active = true
    </select>

    <select id="getAllAssetCategories" resultType="AssetCategory">
        SELECT * FROM asset_categories WHERE is_active = true
    </select>

    <update id="updateAssetCategory">
        UPDATE asset_categories
        SET category_name = #{categoryName},
        description = #{description},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <update id="deleteAssetCategory">
        UPDATE asset_categories SET is_active = false WHERE id = #{id}
    </update>

</mapper>